const StaticPage = () => {
  return (
    <div>
      <h1>Static Page</h1>
      <p>This is a static page that does not require server-side rendering.</p>
    </div>
  );
}

export default StaticPage;